services:
  towteam:
    build: .
    container_name: towteam
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      PORT: 8080
      HOST: 0.0.0.0
      DATABASE_URL: /app/data/towteam.sqlite
    volumes:
      - ./data:/app/data
